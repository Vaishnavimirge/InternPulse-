<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>InternPulse | Login</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f0fbfc;
      color: #333;
      min-height: 100vh;
      padding: 40px 20px;
    }

    .container {
      max-width: 960px;
      margin: 0 auto;
    }

    .login-title {
      font-size: 2.5rem;
      text-align: center;
      margin-bottom: 10px;
      color: #0077b6;
      font-weight: 700;
      letter-spacing: 1px;
    }

    .login-subtitle {
      font-size: 1.1rem;
      text-align: center;
      margin-bottom: 30px;
      color: #555;
    }

    .login-wrapper {
      display: flex;
      flex-wrap: wrap;
      background-color: #ffffff;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
    }

    .panel {
      flex: 1 1 50%;
      padding: 40px 30px;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .add-intern {
      background-color: #d6f5f2;
      border-right: 2px solid #c1ece5;
    }

    .login-intern {
      background-color: #e5f1fb;
    }

    h2 {
      margin-bottom: 24px;
      font-size: 1.8rem;
      color: #023e8a;
      text-align: center;
      font-weight: 600;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    input[type="text"],
    select {
      padding: 14px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 1rem;
      outline: none;
      transition: border-color 0.3s ease;
    }

    input[type="text"]:focus,
    select:focus {
      border-color: #38a3a5;
      background-color: #f0fdfa;
    }

    button {
      padding: 12px;
      background-color: #0077b6;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s ease, transform 0.2s;
    }

    button:hover {
      background-color: #023e8a;
      transform: translateY(-2px);
    }

    @media (max-width: 768px) {
      .panel {
        flex: 1 1 100%;
        border-right: none !important;
        border-bottom: 2px solid #c1ece5;
      }

      .login-wrapper {
        flex-direction: column;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1 class="login-title">🔐 Intern Login & Onboarding Portal</h1>
    <p class="login-subtitle">
      Easily log in to continue your journey or register a new intern to get started with InternPulse.
    </p>

    <div class="login-wrapper">
      <section class="panel add-intern">
        <h2>➕ Add New Intern</h2>
        <form action="/add" method="POST" autocomplete="off">
          <input
            type="text"
            name="name"
            placeholder="Enter intern name"
            required
          />
          <button type="submit">Add Intern</button>
        </form>
      </section>

      <section class="panel login-intern">
        <h2>👤 Login as Existing Intern</h2>
        <form action="/login" method="POST">
          <select name="internId" required>
            <% interns.forEach(intern => { %>
              <option value="<%= intern.id %>"><%= intern.name %></option>
            <% }) %>
          </select>
          <button type="submit">Login</button>
        </form>
      </section>
    </div>
  </div>
</body>
</html>
